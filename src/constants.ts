import { Stock, NewsItem, IPO, MarketIndex } from './types';

// Note: All mock data has been removed to ensure only real NSE API data is used
// The application now relies solely on real-time market data from NSE APIs

export const MARKET_INDICES: MarketIndex[] = [];
export const MOCK_STOCKS: Stock[] = [];
export const MOCK_NEWS: NewsItem[] = [];
export const MOCK_IPOS: IPO[] = [];